import {Link, Outlet} from "react-router-dom";
import Layout from "./Layout";
import React from "react";

const Home = () => {
    return (
        <div>
            <Layout/>
            <h1>HOME</h1>
        </div>

    )
};

export default Home;